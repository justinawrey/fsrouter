{
  "tasks": {
    "example": "deno run --allow-read --allow-net --watch=_scripts/pages _scripts/example.ts",
    "format": "deno fmt",
    "lint": "deno lint",
    "test": "deno test --allow-read --allow-net --watch _test",
    "test-no-watch": "deno test --allow-read --allow-net _test",
  },
  "fmt": {
    "files": {
      // Don't try and format this autogenerated file
      "exclude": ["CHANGELOG.md"]
    }
  }
}
