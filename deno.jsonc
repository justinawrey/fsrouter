{
  "tasks": {
    // note: must provide --cwd to run this from _scripts dir:
    // deno task --cwd _scripts example
    "example": "deno run --allow-read --allow-net --watch=pages example.ts",
    "format": "deno fmt",
    "lint": "deno lint",
    "test": "deno test _test"
  },
  "fmt": {
    "files": {
      // Don't try and format this autogenerated file
      "exclude": ["CHANGELOG.md"]
    }
  }
}
