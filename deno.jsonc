{
  "tasks": {
    "fsrouter": "deno run --allow-read --allow-net --watch=example/pages cli/fsrouter.ts example/pages",
    "format": "deno fmt",
    "lint": "deno lint",
    "test": "deno test --allow-read --allow-net test",
    "bench": "deno bench --unstable bench"
  },
  "fmt": {
    "files": {
      // Don't try and format this autogenerated file
      "exclude": ["CHANGELOG.md"]
    }
  }
}
